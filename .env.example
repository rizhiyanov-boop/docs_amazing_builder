# OpenAI server-side integration (Vercel Function: /api/ai)
# Recommended default: OpenAI nano (cheap and stable).
# Keep the API key only in server env vars (never in frontend code).
OPENAI_API_KEY=sk-proj-REPLACE_ME
OPENAI_MODEL=gpt-4.1-nano
